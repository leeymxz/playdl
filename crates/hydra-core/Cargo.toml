[package]
name = "pdl-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "I/O-free multi-source download scheduler: interval algebra, divergence-triggered repair, liveness"
readme = "README.md"

[dependencies]

[dev-dependencies]
proptest = "1"
criterion = { workspace = true }

[[bench]]
name = "intervals"
harness = false

[lints]
workspace = true